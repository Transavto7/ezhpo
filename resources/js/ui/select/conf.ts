export const Deselect = {
  render: (createElement: any) => createElement('i', { class: ['fa', 'fa-close'] }),
}

export const OpenIndicator = {
  render: (createElement: any) => createElement('i', { class: ['fa', 'fa-chevron-down'] }),
}
